
<body class="monniitor_body">

  <%= form_with(model: moniitor, local: true) do |form| %>
    <% if moniitor.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(moniitor.errors.count, "error") %> prohibited this moniitor from being saved:</h2>

        <ul>
          <% moniitor.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :title %>
      <%= form.text_field :title %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>


</body>
